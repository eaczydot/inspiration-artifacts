import{notFound}from'next/navigation';import{OpenFieldProduct,OpenView}from'../../../../src/inspirations/opensourceui/pages/OpenFieldProduct';const views=['booking','range','countdown','month','week'];export function generateStaticParams(){return views.map(section=>({section}))}export default function Page({params}:{params:{section:string}}){if(!views.includes(params.section))notFound();return <OpenFieldProduct initialView={params.section as OpenView}/>}
