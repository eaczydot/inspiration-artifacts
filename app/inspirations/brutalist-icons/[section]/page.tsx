import{notFound}from'next/navigation';import{BrutalistIconProduct,BrutalView}from'../../../../src/inspirations/brutalist-icons/pages/BrutalistIconProduct';const views=['specimens','xerox','concrete','detail'];export function generateStaticParams(){return views.map(section=>({section}))}export default function Page({params}:{params:{section:string}}){if(!views.includes(params.section))notFound();return <BrutalistIconProduct initialView={params.section as BrutalView}/>}
