import{notFound}from'next/navigation';import{SalesMarketingDashboard,ShadView}from'../../../../src/inspirations/shadcncraft/pages/SalesMarketingDashboard';const views=['overview','analytics','campaigns','deals','leads','customers','settings'];export function generateStaticParams(){return views.map(section=>({section}))}export default function Page({params}:{params:{section:string}}){if(!views.includes(params.section))notFound();return <SalesMarketingDashboard initialView={params.section as ShadView}/>}
