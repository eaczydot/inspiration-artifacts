import{notFound}from'next/navigation';import{MonacoCRM,MonacoView}from'../../../../src/inspirations/monaco/pages/MonacoCRM';const views=['accounts','pipeline','automations','activity','ask'];export function generateStaticParams(){return views.map(section=>({section}))}export default function Page({params}:{params:{section:string}}){if(!views.includes(params.section))notFound();return <MonacoCRM initialView={params.section as MonacoView}/>}
