# opensourceui

A calendar and CRM-map study. It reuses `react-day-picker`, Radix Popover, and Lucide primitives, then adapts the interaction patterns from the source: week-strip availability, unavailable crossed-out times, range selection, account intent markers, and territory planning. The map is a light CSS study so a production MapLibre layer can replace it without changing the surrounding UI.
